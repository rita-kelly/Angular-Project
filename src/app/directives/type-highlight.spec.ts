import { TypeHighlight } from './type-highlight';

describe('TypeHighlight', () => {
  it('should create an instance', () => {
    const directive = new TypeHighlight();
    expect(directive).toBeTruthy();
  });
});
